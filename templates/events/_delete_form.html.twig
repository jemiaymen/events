<form method="post" class="form-horizontal form-material" action="{{ path('app_events_delete', {'id': event.id}) }}" id="del-form">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
    <div class="form-group">
        <div class="col-sm-12">
            <a class="btn btn-danger" data-toggle="modal" data-target="#delModal" >{{ del | trans}}</a>
        </div>
    </div>
</form>


<div id="delModal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">Delete Event</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete this Events ({{event.Name}})</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-info waves-effect" data-dismiss="modal">{{ clo | trans}}</button>
                <button type="button" class="btn btn-danger waves-effect" data-dismiss="modal" onclick="$('#del-form').submit();">{{ del | trans }}</button>
            </div>
        </div>
    </div>
</div>